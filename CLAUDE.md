# Kairos 预测平台 · 个人 Wiki 操作手册（Schema）

> 本文件是这套知识库的 **Schema / 操作手册**（Karpathy「LLM Wiki」范式中的 Layer 3）。
> 任何 LLM 在本仓库工作前，**先读本文件**，然后严格按这里的约定 ingest / 查询 / 维护。
> 本文件与人类共同演化：哪条规则好用就保留，不好用就改。

---

## 0. 这是什么

为一个**正在打造的 Web3 预测平台「Kairos」**建立的个人知识库。
平台**参照 See-X Protocol**（`raw/参考/SEE-X Deck-繁中(空).pdf`）打造：「预测市场 × RWA」事件金融协议。

核心理念（Karpathy gist 442a6bf5…）：**不做 query-time RAG，而是让 LLM 增量地建、并长期维护一个 wiki**。
知识是「持续复利的产物」——每 ingest 一个源，知识库就更完整一点；繁琐的记账（摘要、交叉引用、一致性）交给 LLM。

- **人类负责**：投喂源、指方向、提好问题、做决策。
- **LLM 负责**：其余一切——摘要、建页、交叉引用、保持一致、更新索引与日志。

---

## 1. 三层架构

| 层 | 目录 | 谁能写 | 职责 |
|----|------|--------|------|
| **Layer 1 源** | `raw/` | 只有人类增删；LLM **只读，绝不修改** | 不可变的真相来源（PDF、文章、截图、笔记…） |
| **Layer 2 Wiki** | `wiki/` | LLM 全权维护 | 摘要页、概念页、实体页、平台设计页 + `index.md` + `log.md` |
| **Layer 3 Schema** | `CLAUDE.md`（本文件） | 人类 + LLM 共同演化 | 定义约定与工作流 |

铁律：**`raw/` 只读**。需要把 raw 转成机器可读文本（如 PDF→md 转录）时，转录件也放 `raw/` 内、与原件并列，并视作源的一部分；**不在 `raw/` 写分析**——分析一律进 `wiki/`。

---

## 2. 目录与命名约定

```
wiki/
├── index.md      # 全站目录：按分类列出每一页 + 一行摘要。每次 ingest 必更。
├── log.md        # append-only 时间线：每次 ingest/query/lint 追加一条。
├── 来源/         # 每个 raw 源一页综述（source summary）
├── 概念/         # 概念页：预测市场、CLOB、RWA、预言机…（可跨源复用）
├── 实体/         # 实体页：公司/机构/竞品/监管（Kalshi、Polymarket、CFTC…）
├── 平台/         # 我们 Kairos 平台自身的设计规格页
└── 品牌/         # 命名、视觉风格、logo 资产
```

命名规则：
- 文件名用**中文为主**（Obsidian 友好），英文专名/缩写保留原文（`CLOB.md`、`Kalshi.md`）。
- 一页一主题，**宁多页勿巨页**（单页 ≤ ~400 行）。
- 交叉引用用 `[[wikilink]]`，写**文件名不带扩展名**：`[[预测市场]]`、`[[Kalshi]]`、`[[SES代币]]`。
- 链接到尚不存在的页是允许的——它标记「将来要补的页」，不是错误。

每页顶部用 YAML frontmatter：
```yaml
---
type: 来源 | 概念 | 实体 | 平台 | 品牌
title: 页面标题
status: draft | stable
sources: [see-x-deck]          # 该页依据哪些 raw 源
updated: 2026-06-27
---
```

---

## 3. 页面模板

**来源页 `来源/<slug>.md`**
```
# <源标题>
- 原件：[[raw 路径]]    类型/页数/语言    ingest 日期
## 一句话
## 核心要点（带页码/出处）
## 拆出的页（本源触及/新建了哪些概念·实体·平台页）
## 存疑 / 待核实
```

**概念页 `概念/<名>.md`**：定义 → 关键机制/公式 → 在 Kairos 的应用 → 相关 `[[链接]]` → 来源。

**实体页 `实体/<名>.md`**：是谁/一句话 → 关键事实（估值、合作、监管、时间点，带出处）→ 与预测市场/Kairos 的关系 → 来源。

**平台页 `平台/<模块>.md`**：模块定位 → v0 规格（参照 See-X，标注「可调整」）→ 关键参数/数字 → 开放问题 → 相关 `[[链接]]` → 来源。
> 平台页写「**我们 Kairos 要做什么**」，不是复述 See-X。See-X 的设定作为默认起点，凡我方可能改动处标 `（待定/可调整）`。

---

## 4. 工作流

### 4.1 Ingest（人类往 `raw/` 加了料 → 说「ingest」）
1. **读源**（PDF 用分页读、截图用视觉读）。
2. **与人对要点**：先口头汇报 3–7 条核心 takeaway，确认理解方向。
3. **写来源页** `来源/<slug>.md`。
4. **更新 `index.md`**：把新页登记进目录。
5. **更新相关 概念/实体/平台 页**：新建或补充——一个源通常触及 **10–15 页**。注意回填双向 `[[链接]]`。
6. **追加 `log.md`** 一条：`## [日期] ingest | <源名>` + 触及页清单。
7. **回报**人类：本次新建/修改了哪些页、有何存疑。

### 4.2 Query（人类问平台相关问题）
1. 先查 `index.md` 定位相关页 → 读这些页 → 综合作答（**答案附 `[[来源页]]`**）。
2. 若答案有长期价值 → **回填为新页或并入已有页**，更新 `index.md`，`log.md` 记一条 `query`。

### 4.3 内容生产（人类要「产出内容」：deck/推文/文档/FAQ…）
- 一律**以 wiki 为唯一事实源**取材；产出物默认放 `output/`（非 wiki），wiki 只记「这条结论来自哪些页」。
- 对外材料**当前阶段不提币价/收益承诺式表述**，遵守各对外渠道合规口径。

### 4.4 Lint（人类说「lint」→ 跑知识库体检）
查并修：① 页间**矛盾**；② 被新源**推翻的过时声明**；③ **孤儿页**（无入链）；④ 缺失的交叉引用；⑤ 数据缺口（该有数字却没有）。产出一份问题清单，逐条提议修法，`log.md` 记一条 `lint`。

---

## 5. 当前状态（随项目演化，必要时更新本节）
- 平台名 **Kairos**，ticker **$KAI**。蓝图源 = See-X deck（`来源/see-x-deck`）。
- 品牌视觉：BGB 白皮书的 3D 风格（黑底 + 写实玻璃/金属 3D + 彩散），**主色币安金 `#F0B90B`/`#FCD535`**（非 cyan）。详见 `[[视觉风格]]`。
- Gemini key 在本地 `.env`（`GEMINI_API`）——**仅本地用，绝不写进任何对外材料或提交**。

---

## 6. 给 LLM 的硬性提醒
- 动笔前**先读 `index.md`**，避免重复建页。
- 改一处事实，**回头修所有引用它的页**（用搜索找 `[[该页]]`）。
- 数字必须可溯源——写清来自哪页/哪张图。
- 不确定就标 `（待核实）`，不要编。
- `raw/` 永远只读。
